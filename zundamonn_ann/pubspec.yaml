name: zundamonn_ann
description: "ずんだもんのオールナイトニッポン"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.2.1
  http: ^1.2.2
  audioplayers: ^6.1.0
  just_audio: ^0.9.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # assets エントリを追加してアセット画像のパスを指定
  assets:
    - assets/images/zundamon.jpg
    - .env
